package constant

const(
	UrlShortnerPath = "/short"

	RedirectUrlPath = "/url/:code"
)

const (
	Database = "urlshortner" 

	UrlCollection = "url"
)

const (
	BindError = "there is something wrong while backing"
)

const (	
	BaseUrl = "localhost:8000/"
)